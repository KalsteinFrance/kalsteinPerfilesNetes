jQuery(document).ready(function($){
  // 1. Cargar los datos de la tabla
  // 2. Cargar los datos de la tabla en el select
  // 3. Cargar los datos en el formulario
  // 4. Actualizar los datos de la tabla
  // 5. Actualizar los datos de la tabla en el select

  $(document).ready(function() {
  $('#dataTable').DataTable();
});
 
  
 
  });
  
